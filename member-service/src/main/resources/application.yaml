spring:
  application:
    name: member-service

  datasource:
    url: jdbc:postgresql://localhost:5432/member_db
    username: seongmoasm

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

logging:
  level:
    com.example.reviewservice: DEBUG